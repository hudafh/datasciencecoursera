polluttantmean <- function(directory, pollutant, id) {
  t <-1
          i <- 1
          j <- 1
          k <- 1
          totalLength <- 0
          #for(i in id){
            #print(id)
            pasta <- dir(directory)   #pasta contains the list of files inside the directory
            
            
        
               
           for (t in id) {
                          
                print(t)
                count <- nrow(read.csv(pasta[t]))
                totalLength <- totalLength + count
                          
          }
           print(totalLength)
        
           dataNrs <- vector(length = totalLength)
        
             
             
          for (i in seq_along(id)) {
              
              temp <- read.csv(pasta[i])  #read file
              
              for (j in 1:nrow(temp)) {     #for every line in the file that was read
                #if(pollutant == "sulfate") {
                dataNrs[k] <- temp[j,3]     #copy the line to the array the will have the actual data measurement
                k <- k +1                 #increment arrat pointer
                #}
                
                #if(pollutant == "nitrate") {
                 # dataNrs[k] <- temp[j,3]     #copy the line to the array the will have the actual data measurement
                  #k <- k +1                 #increment arrat pointer
                #}
              }
            }
            print(dataNrs[3])
            #bad <- is.na(dataNrs)
            #cleanValues <- dataNrs[!bad]
            #print(length(cleanValues))
            
            #print(cleanValues)
            #for (i in 1:10)
             # print(cleanValues[i])
            #polMean <- mean(cleanValues)
            #print(mean.default(cleanValues,na.rm = TRUE))
            #print(polMean)
       
              
}
